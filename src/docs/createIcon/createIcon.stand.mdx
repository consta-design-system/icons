```tsx
import { createIcon } from '@consta/icons/Icon';
```

Если вы не нашли подходящую иконку в [нашей библиотеке иконок](##LIBS.LIB.STAND/lib:icons/stand:icons-icon-stable), можете создать свою с помощью функции `createIcon`. Передайте в неё рендер-функцию и необходимые параметры.
