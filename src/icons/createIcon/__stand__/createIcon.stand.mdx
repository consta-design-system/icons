```tsx
import { createIcon } from '@consta/icons/createIcon';
```

Если вы не смогли подобрать необходимую вам иконку вы можете создать ее сами.

Для того чтобы создать свою иконку необходимо использовать функцию `createIcon`, передав в нее рендер функцию и неообходимые параметры.
